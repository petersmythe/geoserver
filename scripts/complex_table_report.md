# Complex Tables Requiring Manual Intervention

Generated by analysis of `detect_table_issues.py` output after all automated fixes (Phase 3).

## Summary

41 remaining issues across 5 files, all in the "continuation row with empty first cell" category.
These are tables that use **intentional row-spanning** patterns from RST grid tables — the
empty first cells represent merged/spanned rows in the original RST. The Markdown conversion
correctly preserved the visual row-spanning using empty cells, which is valid pipe-table syntax.

## Severity Classification

### Priority 1 — Structural/Rendering Problem (needs fix)

#### 1. `doc/en/user/extensions/netcdf/netcdf.md` (lines 49–55)

- **Issue**: Table has `>` prefix artifacts in header and data cells (e.g., `> Runtime`, `> Time`, `> 0`, `> XXXX`). These `>` characters are RST grid-table alignment artifacts that were not cleaned during conversion. They render as literal `>` text in the output.
- **Original RST**: Grid table with merged header cells (`Runtime` spans 2 cols, `Time` spans 3 cols). The `>` prefixes do not exist in the RST source.
- **Severity**: HIGH — content is visually corrupted with stray `>` characters
- **Fix needed**: Remove `>` prefixes from all cells. The header row also has incorrect column count (2 columns in header vs 5 in data rows) due to the original RST using merged cells. Consider converting to HTML table with `colspan` to properly represent the merged headers, or restructure as a simpler 5-column pipe table with repeated header text.
- **Requirements**: 1.22, 2.22

### Priority 2 — Intentional Row-Spanning (valid, no fix needed)

These tables use empty first-column cells to represent row groups. This is a standard Markdown
pipe-table pattern for visual row-spanning and renders correctly in MkDocs.

#### 2. `doc/en/user/extensions/geofence-server/rest-batch-op.md` (lines 95–101)

- **Issue**: POST method row spans 4 response codes (200, 400, 404, 500). Empty Method/Action cells on continuation rows.
- **Original RST**: Grid table with row-spanning on Method and Action columns.
- **Severity**: NONE — renders correctly. The empty cells visually group the response codes under the single POST method, matching the RST intent.
- **Fix needed**: None. This is valid and renders as intended.

#### 3. `doc/en/user/extensions/mapml/installation.md` (lines 163–200)

- **Issue**: 30 continuation rows with empty Symbolizer column. Three symbolizer groups (PointSymbolizer, LineSymbolizer, PolygonSymbolizer) each have multiple style attributes.
- **Original RST**: Grid table with row-spanning on Symbolizer column.
- **Severity**: NONE — renders correctly. The empty first cells group style attributes under their parent symbolizer, matching the RST intent exactly.
- **Fix needed**: None. This is valid and renders as intended.

#### 4. `doc/en/user/installation/war.md` (lines 10–15)

- **Issue**: 3 continuation rows with empty JavaEE column. JakartaEE rows (Servlet API 6.1, 6.0) and one row with both JavaEE and JakartaEE empty.
- **Original RST**: Grid table with row-spanning on JavaEE column.
- **Severity**: NONE — renders correctly. The empty cells represent that JakartaEE entries don't have a JavaEE equivalent, which is the intended meaning.
- **Fix needed**: None. This is valid and renders as intended.

#### 5. `doc/en/user/styling/ysld/reference/symbolizers/index.md` (line 67)

- **Issue**: Header row has empty first cell (`|  | Points | Lines | ...`).
- **Original RST**: `list-table` directive with `:stub-columns: 1` and empty first header cell.
- **Severity**: NONE — renders correctly. The empty top-left cell is intentional since the first column contains row headers (symbolizer names) and the first row contains column headers (geometry types).
- **Fix needed**: None. This is valid and renders as intended.

## Action Items

| # | File | Severity | Action |
|---|------|----------|--------|
| 1 | `extensions/netcdf/netcdf.md` | HIGH | Remove `>` artifacts, fix header colspan |
| 2 | `extensions/geofence-server/rest-batch-op.md` | NONE | No action needed |
| 3 | `extensions/mapml/installation.md` | NONE | No action needed |
| 4 | `installation/war.md` | NONE | No action needed |
| 5 | `styling/ysld/reference/symbolizers/index.md` | NONE | No action needed |

## Conclusion

Of the 41 flagged issues, only 1 file (`netcdf.md`) has an actual rendering problem requiring
manual repair. The remaining 40 issues across 4 files are false positives — they use intentional
empty-cell row-spanning which is valid Markdown pipe-table syntax and renders correctly.
